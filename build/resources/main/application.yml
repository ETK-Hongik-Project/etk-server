spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/etk?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
    username: root
    password: jeonmin7721
  jackson:
    time-zone: Asia/Seoul
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    hibernate:
      ddl-auto: update
      show-sql: true
      format_sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
      hibernate:
        default_batch_fetch_size: 10


  data:
    redis:
      port: 6379
      host: localhost

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: JguATATjx0ihfBEbKJJn
            client-secret: 3pNx_fwkPl
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "http://localhost:8080/login/oauth2/code/naver"
            scope:
              - name
              - email
              - mobile
            client-name: Naver

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-info-authentication-method: header
            user-name-attribute: response # Naver 응답 값 resultCode, message, response 중 response 지정

logging:
  level:
    org.hibernate.SQL: debug

upload:
  directory: /Users/min/Documents/study/etk/server-file

jwt:
  secret: b89b8ccc063e178086760225d01577c3de99f5709ae73b8f8cbc45aa733494130d86c5f585f3a2fd3d6a198b5b4a682740289be20351487bfd5852b6e61333a8
  validationTime: 3600
